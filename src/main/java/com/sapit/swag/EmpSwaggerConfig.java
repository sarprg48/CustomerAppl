package com.sapit.swag;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//import static springfox.documentation.builders.PathSelectors.regex;
//import static springfox.documentation.builders.RequestHandlerSelectors.basePackage;
//import springfox.documentation.spi.DocumentationType;
//import springfox.documentation.spring.web.plugins.Docket;
//import springfox.documentation.swagger2.annotations.EnableSwagger2;
//import springfox.documentation.swagger2.annotations.EnableSwagger2WebMvc;

//@Configuration
//@EnableSwagger2
public class EmpSwaggerConfig {
	
//	@Bean
//	public void createswag() {
		/*return new Docket(DocumentationType.SWAGGER_2).
				select().
				apis(basePackage("com.sapit.controller")).
				paths(regex("/api.*")).build();
	}*/
	
	/*
	 * @Bean public Docket createswag() { return new
	 * Docket(DocumentationType.SWAGGER_2).select().
	 * apis(RequestHandlerSelectors.basePackage("com.sapit.controller")).
	 * paths(PathSelectors.regex("/api.*")).build(); }
	 */
}
